# Templates

For a new experiment:
1. Make a copy of `yyyy-mm-dd-descriptor.ipynb` in the root of the repository (one level up from here)
2. Follow the instructions included at the top of `yyyy-mm-dd-descriptor.ipynb`